eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8086
spring:
  application:
    name: cart
  data:
    redis:
      host: localhost
      port: 6379
management:
  tracing:
    sampling:
      probability: 1.0
